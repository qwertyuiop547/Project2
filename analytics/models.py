"""
Analytics models - mostly computed from other models
"""
from django.db import models

# Analytics app mainly uses aggregation from other models
# No specific models needed, but keeping file for consistency

